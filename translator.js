export const translateMorseToEnglish = (input) => {
  const input = inputElement.innerHTML;
  const output = outputElement.innerHTML;
};
export const translateEnglishToMorse = (input) => {
  const input = inputElement.innerHTML;
  const output = outputElement.innerHTML;
};

const translateButton = document.getElementById("translate__btn");
const inputElement = document.getElementById("input__box");
const outputElement = document.getElementById("output__box");
const clearButton = document.getElementById("clear__btn");
